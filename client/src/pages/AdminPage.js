import React from 'react';
import {observer} from "mobx-react-lite";

const AdminPage = () => {
    return (
        <div>
            AdminPage
        </div>
    );
};

export default observer(AdminPage);
